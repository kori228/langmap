# Wordmap per-word file split — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transpose wordmap word data from inline-per-language storage in `wordmap_data.js` into per-concept files under `words/`, with an ordered manifest, eager parallel loading, validator coverage, and contributor docs in EN+JA.

**Architecture:** A one-time Node migration script reads the current `wordmap_data.js` under Node and emits 20 self-contained per-concept files (`words/<id>.js`), plus a stripped language-meta-only `wordmap_data.js` and a `word_manifest.js` ordering file. `wordmap.html` gains a tiny inline loader that synthesizes one `<script>` per concept in parallel, then merges the loaded `WORDS` global back into `LANG_DATA[code].words` so all downstream consumers keep working unchanged.

**Tech Stack:** Vanilla JavaScript (no build step), Node 18+ for one-off scripts, the existing `validate_wordmap_data.js` for invariant checks.

**Spec:** [docs/superpowers/specs/2026-05-18-wordmap-per-word-files-design.md](../specs/2026-05-18-wordmap-per-word-files-design.md)

---

## File structure

**Created**
- `tools/migrate_inline_words_to_files.mjs` — one-shot transposition
- `tools/generate_lang_codes_md.mjs` — regenerates `LANG_CODES.md`
- `word_manifest.js` — `const WORD_ORDER = [...]`
- `words/<id>.js` × 20 — generated by migration; one file per concept
- `docs/words/CONTRIBUTING.md` — English contributor manual
- `docs/words/CONTRIBUTING.ja.md` — Japanese contributor manual
- `docs/words/LANG_CODES.md` — 934-language code reference (generated)

**Modified**
- `wordmap_data.js` — `WORD_LIST` removed; each `LANG_DATA[code].words` and `.altWordForms` removed
- `wordmap.html` — `WM_ASSET_VERSION.words` added, `WM_ASSET_VERSION.data` bumped, loader + merge logic added
- `validate_wordmap_data.js` — new checks; `ASSET_KEY_BY_PATH` gains glob support
- `README.md` — short pointer to `docs/words/`

**Git tag**
- `pre-word-split` placed on the commit preceding the migration commit, so the inline-words state is one `git reset --hard pre-word-split` away.

---

## Task 1: Land migration script skeleton

**Files:**
- Create: `tools/migrate_inline_words_to_files.mjs`

- [ ] **Step 1: Create the skeleton**

```js
// tools/migrate_inline_words_to_files.mjs
//
// One-shot script. Loads the inline-style wordmap_data.js under Node,
// transposes each (language, word) cell into per-concept files under
// words/, and emits a stripped wordmap_data.js plus word_manifest.js.
//
// Run from repo root:   node tools/migrate_inline_words_to_files.mjs
//
// Leaves a backup at wordmap_data.js.pre-split for manual diff.

import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');

function loadDataModule() {
    const src = fs.readFileSync(path.join(ROOT, 'wordmap_data.js'), 'utf8');
    const sandbox = {};
    vm.createContext(sandbox);
    vm.runInContext(src, sandbox);
    if (!sandbox.WORD_LIST || !sandbox.LANG_DATA) {
        throw new Error('wordmap_data.js did not define WORD_LIST + LANG_DATA');
    }
    return { WORD_LIST: sandbox.WORD_LIST, LANG_DATA: sandbox.LANG_DATA };
}

function main() {
    const { WORD_LIST, LANG_DATA } = loadDataModule();
    console.log(`loaded ${WORD_LIST.length} words, ${Object.keys(LANG_DATA).length} languages`);
}
main();
```

- [ ] **Step 2: Run to verify it loads the data**

Run: `node tools/migrate_inline_words_to_files.mjs`
Expected output: `loaded 20 words, 934 languages` (counts may differ if data changed since this plan was written; flag any mismatch).

- [ ] **Step 3: Commit**

```bash
git add tools/migrate_inline_words_to_files.mjs
git commit -m "Wordmap migrate: skeleton loader for inline → per-word-file transpose"
```

---

## Task 2: Transpose word data into in-memory WORDS object

**Files:**
- Modify: `tools/migrate_inline_words_to_files.mjs`

- [ ] **Step 1: Add the transposition pass**

Append before `main()`:

```js
function buildWords(WORD_LIST, LANG_DATA) {
    // Result keyed by word ID. Each value:
    //   { label, definition, data: { langCode: tuple|object } }
    const WORDS = {};
    for (const w of WORD_LIST) {
        WORDS[w.id] = {
            label:      w.label      || {},
            definition: w.definition || {},
            data:       {},
        };
    }
    for (const [code, lang] of Object.entries(LANG_DATA)) {
        const words    = lang.words        || {};
        const altForms = lang.altWordForms || {};
        for (const wid of Object.keys(WORDS)) {
            const tuple = words[wid];
            const alt   = altForms[wid];
            if (!tuple && !alt) continue;
            if (alt && alt.length > 0) {
                // Object form: keeps the primary tuple plus alt scripts.
                WORDS[wid].data[code] = {
                    form: tuple ? tuple[0] : '',
                    ipa:  tuple ? tuple[1] : '',
                    alt:  alt,
                };
            } else if (tuple) {
                WORDS[wid].data[code] = tuple;
            }
        }
    }
    return WORDS;
}
```

Update `main()`:

```js
function main() {
    const { WORD_LIST, LANG_DATA } = loadDataModule();
    const WORDS = buildWords(WORD_LIST, LANG_DATA);
    console.log(`built ${Object.keys(WORDS).length} word entries`);
    for (const w of WORD_LIST) {
        const filled = Object.keys(WORDS[w.id].data).length;
        console.log(`  ${w.id}: ${filled} translations`);
    }
}
main();
```

- [ ] **Step 2: Run, eyeball the counts**

Run: `node tools/migrate_inline_words_to_files.mjs`
Expected: 20 lines like `  water: 870 translations`. Numbers should be close to total language count minus the few langs with explicit `'—'` dashes.

- [ ] **Step 3: Commit**

```bash
git add tools/migrate_inline_words_to_files.mjs
git commit -m "Wordmap migrate: build in-memory WORDS object (transpose + alt-form fold)"
```

---

## Task 3: Serialize per-word files

**Files:**
- Modify: `tools/migrate_inline_words_to_files.mjs`

- [ ] **Step 1: Add the serializer**

```js
function indent(n) { return ' '.repeat(n); }

function jsValue(v) {
    // Conservative JSON.stringify-based serializer. Strings → JSON
    // strings; arrays → JSON arrays; objects → JSON objects. JSON is
    // a valid subset of JS literals for these payloads (no functions,
    // no Date, no undefined values flowing through).
    return JSON.stringify(v);
}

function serializeWord(id, entry) {
    const lines = [];
    lines.push('/**');
    lines.push(` * ${id} — see docs/words/CONTRIBUTING.md for editing guidance.`);
    lines.push(' * Language codes: see docs/words/LANG_CODES.md.');
    lines.push(' */');
    lines.push(`WORDS.${id} = {`);

    // label
    lines.push(`${indent(2)}label: {`);
    for (const [k, v] of Object.entries(entry.label)) {
        lines.push(`${indent(4)}${k}: ${jsValue(v)},`);
    }
    lines.push(`${indent(2)}},`);

    // definition
    lines.push(`${indent(2)}definition: {`);
    for (const [k, v] of Object.entries(entry.definition)) {
        lines.push(`${indent(4)}${k}: ${jsValue(v)},`);
    }
    lines.push(`${indent(2)}},`);

    // data
    lines.push(`${indent(2)}data: {`);
    for (const [code, val] of Object.entries(entry.data)) {
        if (Array.isArray(val)) {
            lines.push(`${indent(4)}${code}: [${jsValue(val[0] ?? '')}, ${jsValue(val[1] ?? '')}],`);
        } else {
            // Object form: pretty-print on its own block.
            lines.push(`${indent(4)}${code}: {`);
            lines.push(`${indent(6)}form: ${jsValue(val.form ?? '')},`);
            lines.push(`${indent(6)}ipa:  ${jsValue(val.ipa  ?? '')},`);
            if (val.alt) {
                lines.push(`${indent(6)}alt: [`);
                for (const a of val.alt) {
                    lines.push(`${indent(8)}{ form: ${jsValue(a.form)}, script: ${jsValue(a.script)}, source: ${jsValue(a.source ?? '')} },`);
                }
                lines.push(`${indent(6)}],`);
            }
            lines.push(`${indent(4)}},`);
        }
    }
    lines.push(`${indent(2)}},`);
    lines.push('};');
    lines.push('');
    return lines.join('\n');
}

function writePerWordFiles(WORDS) {
    const outDir = path.join(ROOT, 'words');
    fs.mkdirSync(outDir, { recursive: true });
    for (const [id, entry] of Object.entries(WORDS)) {
        const file = path.join(outDir, `${id}.js`);
        fs.writeFileSync(file, serializeWord(id, entry), 'utf8');
        console.log(`wrote ${file}`);
    }
}
```

Wire it into `main()`:

```js
function main() {
    const { WORD_LIST, LANG_DATA } = loadDataModule();
    const WORDS = buildWords(WORD_LIST, LANG_DATA);
    writePerWordFiles(WORDS);
}
main();
```

- [ ] **Step 2: Run + spot-check one file**

Run: `node tools/migrate_inline_words_to_files.mjs`
Expected: 20 lines `wrote .../words/<id>.js`.
Then inspect: `head -40 words/water.js` — confirm it has the `WORDS.water = {…}` shape with `label`, `definition`, `data` blocks.

- [ ] **Step 3: Commit**

```bash
git add tools/migrate_inline_words_to_files.mjs words/
git commit -m "Wordmap migrate: serialize per-word files under words/"
```

---

## Task 4: Generate word_manifest.js + stripped wordmap_data.js

**Files:**
- Modify: `tools/migrate_inline_words_to_files.mjs`

- [ ] **Step 1: Add manifest writer**

```js
function writeManifest(WORD_LIST) {
    const lines = [
        '// word_manifest.js — display order of word concepts.',
        '// Append a new ID here when adding words/<id>.js.',
        '',
        'const WORD_ORDER = [',
        ...WORD_LIST.map(w => `${indent(4)}${JSON.stringify(w.id)},`),
        '];',
        '',
    ];
    fs.writeFileSync(path.join(ROOT, 'word_manifest.js'), lines.join('\n'), 'utf8');
    console.log('wrote word_manifest.js');
}
```

- [ ] **Step 2: Add stripped-data writer**

```js
function writeStrippedData() {
    const file = path.join(ROOT, 'wordmap_data.js');
    const original = fs.readFileSync(file, 'utf8');
    fs.writeFileSync(file + '.pre-split', original, 'utf8');

    // 1. Remove the leading `const WORD_LIST = [...];` block (with its
    //    trailing semicolon and the comment line directly above it).
    // 2. From each `LANG_DATA[…].words = {…}` and `.altWordForms = {…}`
    //    block, strip the field. Sources for the regexes here:
    //      WORD_LIST   — bracketed array, multiline
    //      words: {…}  — single line per language; nested {…}
    //      altWordForms: {…} — same
    let out = original;
    out = out.replace(/(^|\n)const WORD_LIST = \[[\s\S]*?\n\];\n/, '\n');
    out = out.replace(/,?\s*words:\s*\{[^{}]*(?:\{[^{}]*\}[^{}]*)*\}/g, '');
    out = out.replace(/,?\s*altWordForms:\s*\{[^{}]*(?:\{[^{}]*\}[^{}]*)*\}/g, '');
    // Tidy: collapse `, ,` and stray trailing commas inside `{}`.
    out = out.replace(/,\s*,/g, ',').replace(/\{\s*,/g, '{');
    fs.writeFileSync(file, out, 'utf8');
    console.log('wrote stripped wordmap_data.js (backup at wordmap_data.js.pre-split)');
}
```

- [ ] **Step 3: Wire and run**

```js
function main() {
    const { WORD_LIST, LANG_DATA } = loadDataModule();
    const WORDS = buildWords(WORD_LIST, LANG_DATA);
    writePerWordFiles(WORDS);
    writeManifest(WORD_LIST);
    writeStrippedData();
}
main();
```

Run: `node tools/migrate_inline_words_to_files.mjs`

- [ ] **Step 4: Verify stripped file still parses**

Run:
```bash
node -e "global.LANG_DATA=null; require('./wordmap_data.js'); console.log(Object.keys(global.LANG_DATA).length, 'langs after strip')"
```
Expected: `934 langs after strip` (or current count). No syntax errors.

If parsing fails, inspect `wordmap_data.js.pre-split` for the original and adjust the regexes in `writeStrippedData()`.

- [ ] **Step 5: Commit**

```bash
git add tools/migrate_inline_words_to_files.mjs word_manifest.js wordmap_data.js wordmap_data.js.pre-split
git commit -m "Wordmap migrate: strip inline words + emit word_manifest.js"
```

---

## Task 5: Tag the pre-migration state

This task records a rollback point. The previous commit already strips
`wordmap_data.js`, so the tag points to the commit BEFORE that one.

- [ ] **Step 1: Tag the parent of the strip commit**

```bash
git tag pre-word-split HEAD~1
git tag -l pre-word-split -n5
```

Expected: shows the migration-write commit (Task 3 commit, before the strip in Task 4).

- [ ] **Step 2: Verify rollback works (then come back)**

```bash
git stash      # in case there are uncommitted changes
git checkout pre-word-split -- wordmap_data.js
grep -c "words: {" wordmap_data.js
```

Expected: a number > 900 (inline `words: {…}` blocks restored from tag).

```bash
git checkout HEAD -- wordmap_data.js   # restore the stripped state
grep -c "words: {" wordmap_data.js     # 0
git stash pop || true
```

- [ ] **Step 3: Delete the backup file from the working tree**

The `wordmap_data.js.pre-split` backup is now redundant (tag covers it).
Keep it out of git:

```bash
git rm --cached wordmap_data.js.pre-split 2>/dev/null || true
rm -f wordmap_data.js.pre-split
echo 'wordmap_data.js.pre-split' >> .gitignore
git add .gitignore
git commit -m "Wordmap migrate: tag pre-word-split, drop redundant backup file"
```

---

## Task 6: Add WORDS global + WM_ASSET_VERSION.words key

**Files:**
- Modify: `wordmap.html` (locate the existing `const WM_ASSET_VERSION = { … };` block and the `<script src="wordmap_data.js?v=…">` line)

- [ ] **Step 1: Add the new version key + bump data**

Find the registry (currently around `WM_ASSET_VERSION = { styles: 78, data: 213, … }`) and edit:

```js
const WM_ASSET_VERSION = {
    styles:    78,
    data:     214,        // bumped: words/altWordForms stripped
    metaI18n:  15,
    filter:    31,
    names:    108,
    meta:     190,
    trivia:    27,
    triviaI18n: 3,
    words:      1,        // NEW: covers word_manifest.js + words/*.js
};
```

- [ ] **Step 2: Add `window.WORDS = {}` at the very top of the IIFE**

Inside `<script>` near the top of `wordmap.html`, before any code that references words, insert:

```js
window.WORDS = window.WORDS || {};
```

This ensures `WORDS.water = …` assignments in per-word files have a target object even if the file loads before the main IIFE runs.

- [ ] **Step 3: Commit**

```bash
git add wordmap.html
git commit -m "Wordmap: reserve WORDS global + WM_ASSET_VERSION.words slot"
```

---

## Task 7: Insert manifest + per-word-loader script block

**Files:**
- Modify: `wordmap.html` (locate the existing `<script src="wordmap_data.js?v=…">` line, near the top of `<body>`)

- [ ] **Step 1: Add `word_manifest.js` + loader after `wordmap_data.js`**

Find the existing line:

```html
<script src="wordmap_data.js?v=214"></script>
```

Insert immediately after it:

```html
<script src="word_manifest.js?v=1"></script>
<script>
    // Synthesize one <script> tag per concept in WORD_ORDER. The browser
    // downloads them in parallel; each file just writes to a different
    // WORDS[id] slot so load order doesn't matter. We dispatch
    // `langmap:words-loaded` once every file has loaded; the main app
    // waits for it before constructing LANG_DATA[*].words lookups.
    (function () {
        if (typeof WORD_ORDER === 'undefined') {
            console.error('WORD_ORDER not defined — word_manifest.js failed to load');
            return;
        }
        const v = (window.WM_ASSET_VERSION && window.WM_ASSET_VERSION.words) || 1;
        const remaining = new Set(WORD_ORDER);
        function done(id) {
            remaining.delete(id);
            if (remaining.size === 0) {
                document.dispatchEvent(new Event('langmap:words-loaded'));
            }
        }
        for (const id of WORD_ORDER) {
            const s = document.createElement('script');
            s.src     = `words/${id}.js?v=${v}`;
            s.onload  = () => done(id);
            s.onerror = () => {
                console.error('failed to load word', id);
                done(id);   // never block the boot on a single missing file
            };
            document.head.appendChild(s);
        }
    })();
</script>
```

Note: `WM_ASSET_VERSION` is defined inside the main IIFE, not on `window`. To make it readable from this loader we'll also expose it — handled in Task 8.

- [ ] **Step 2: Commit**

```bash
git add wordmap.html
git commit -m "Wordmap: inline loader injects <script> per concept from WORD_ORDER"
```

---

## Task 8: Expose WM_ASSET_VERSION + add runtime merge

**Files:**
- Modify: `wordmap.html` (the main IIFE that hosts `LANG_DATA`-using code)

- [ ] **Step 1: Expose the version registry to the loader**

Locate `const WM_ASSET_VERSION = { … };` and add immediately after:

```js
window.WM_ASSET_VERSION = WM_ASSET_VERSION;
```

This is read-only sharing for the inline loader inserted in Task 7.

- [ ] **Step 2: Add the merge helper before the main boot code**

After `WORDS` and `LANG_DATA` are both expected to exist, add:

```js
function _mergeWordsIntoLangData() {
    if (typeof WORD_ORDER === 'undefined' || !window.WORDS) return;
    for (const id of WORD_ORDER) {
        const w = window.WORDS[id];
        if (!w || !w.data) continue;
        for (const [code, entry] of Object.entries(w.data)) {
            const lang = LANG_DATA[code];
            if (!lang) continue;
            lang.words = lang.words || {};
            if (Array.isArray(entry)) {
                lang.words[id] = entry;
            } else {
                lang.words[id] = [entry.form || '', entry.ipa || ''];
                if (entry.alt && entry.alt.length) {
                    lang.altWordForms = lang.altWordForms || {};
                    lang.altWordForms[id] = entry.alt;
                }
            }
        }
    }
    // Backwards-compatible WORD_LIST shape for any consumer that reads it.
    window.WORD_LIST = WORD_ORDER.map(id => {
        const w = window.WORDS[id] || {};
        return { id, label: w.label || {}, definition: w.definition || {} };
    });
}
```

- [ ] **Step 3: Gate the boot on `langmap:words-loaded`**

Wrap the existing boot logic so it doesn't run before the words are merged. Locate where the main DOMContentLoaded/init code begins and prepend:

```js
function _bootWordmap() {
    _mergeWordsIntoLangData();
    // …existing boot body…
}
if (window.WORDS && WORD_ORDER && Object.keys(window.WORDS).length >= WORD_ORDER.length) {
    // Already loaded (fast path: scripts resolved before this code ran).
    _bootWordmap();
} else {
    document.addEventListener('langmap:words-loaded', _bootWordmap, { once: true });
}
```

(Exact placement: wherever today's code currently calls the first function that depends on `WORD_LIST` or `LANG_DATA[code].words[…]`.)

- [ ] **Step 4: Smoke test in browser**

Run: `python3 server.py` (in another terminal if not already running).
Open `http://localhost:8001/wordmap.html`.

Expected behaviour:
- Word selector populated with the 20 concepts in the original order.
- Picking "water" / "fire" / etc. renders markers identically to before the split.
- DevTools Network tab shows 20 parallel requests for `words/<id>.js?v=1`.
- DevTools Console is free of red errors.

If anything misrenders, compare with the `pre-word-split` tag to isolate.

- [ ] **Step 5: Commit**

```bash
git add wordmap.html
git commit -m "Wordmap: runtime merge of WORDS → LANG_DATA[*].words + WORD_LIST shim"
```

---

## Task 9: Validator — extend ASSET_KEY_BY_PATH to support globs

**Files:**
- Modify: `validate_wordmap_data.js`

- [ ] **Step 1: Add a glob entry + extend the walker**

Find `const ASSET_KEY_BY_PATH = { … };` (around line 566) and replace:

```js
const ASSET_KEY_BY_PATH = {
    'styles.css':         'styles',
    'wordmap_data.js':    'data',
    'meta_i18n_ext.js':   'metaI18n',
    'lang-filter.js':     'filter',
    'lang_names.js':      'names',
    'wordmap_meta.js':    'meta',
    'wordmap_trivia.js':  'trivia',
    'word_manifest.js':   'words',
    'words/*.js':         'words',   // glob: every concept file
};
```

Then find the loop `for (const [path, key] of Object.entries(ASSET_KEY_BY_PATH)) { … }` (around line 590) and update its `<script src>` regex builder so that a path containing `*` is treated as a wildcard. Replace the body with something like:

```js
for (const [pathPattern, key] of Object.entries(ASSET_KEY_BY_PATH)) {
    if (registry[key] === undefined) {
        driftReporter(`[#19] WM_ASSET_VERSION missing key '${key}' for ${pathPattern}`);
        continue;
    }
    if (pathPattern.includes('*')) {
        // Glob check: every <script src="words/<id>.js?v=N"> must use the
        // current registry value. Scanning is done with a regex built
        // from the pattern.
        const re = new RegExp(
            `<script[^>]+src=["']${pathPattern.replace(/[.+]/g, '\\$&').replace('*', '[^"\']+')}\\?v=(\\d+)["']`,
            'g',
        );
        let m;
        while ((m = re.exec(htmlSrc)) !== null) {
            if (+m[1] !== registry[key]) {
                driftReporter(`[#19] ${m[0]}: ?v=${m[1]} != WM_ASSET_VERSION.${key}=${registry[key]}`);
            }
        }
        // Synthesized loader path: any words/${id}.js?v=${variable} is OK if
        // it's referenced via the loader. Skip — we trust the loader.
        continue;
    }
    // (existing exact-path handling stays here — keep what's currently
    //  between line ~595 and the wordmap_meta.js dynamic block)
}
```

Keep all the wordmap_meta.js dynamic-URL handling underneath unchanged.

- [ ] **Step 2: Run the validator**

```bash
node validate_wordmap_data.js
```

Expected: no new failures introduced by the glob support. Existing checks unchanged.

- [ ] **Step 3: Commit**

```bash
git add validate_wordmap_data.js
git commit -m "Validator: glob support in ASSET_KEY_BY_PATH (words/*.js → 'words')"
```

---

## Task 10: Validator — manifest + WORDS structural checks

**Files:**
- Modify: `validate_wordmap_data.js`

- [ ] **Step 1: Add the new check block**

Find a stable insertion point (after `checkCounts` is run or after the cache-version block) and append:

```js
// ---------- per-word file split (Audit Task 235) ------------------
function checkPerWordSplit() {
    const manifestPath = 'word_manifest.js';
    const wordsDir     = 'words';

    if (!fs.existsSync(manifestPath)) {
        E(`[#WS1] ${manifestPath} not found`);
        return;
    }
    if (!fs.existsSync(wordsDir)) {
        E(`[#WS2] ${wordsDir}/ not found`);
        return;
    }

    // Read manifest by evaluating it in a vm sandbox.
    const sandbox = {};
    vm.createContext(sandbox);
    vm.runInContext(fs.readFileSync(manifestPath, 'utf8'), sandbox);
    const order = sandbox.WORD_ORDER;
    if (!Array.isArray(order)) {
        E(`[#WS3] ${manifestPath} did not define WORD_ORDER as an array`);
        return;
    }

    // Load every WORDS.<id> by running each file in a shared sandbox
    // that already has window.WORDS pre-seeded.
    const wsandbox = { window: {} };
    wsandbox.window.WORDS = wsandbox.WORDS = {};
    vm.createContext(wsandbox);
    const files = fs.readdirSync(wordsDir).filter(f => f.endsWith('.js'));
    for (const f of files) {
        vm.runInContext(fs.readFileSync(path.join(wordsDir, f), 'utf8'), wsandbox);
    }
    const WORDS = wsandbox.WORDS;

    // (1) Forward coverage: every manifest ID has a file + WORDS entry.
    for (const id of order) {
        if (!WORDS[id]) E(`[#WS4] WORD_ORDER lists '${id}' but words/${id}.js did not define WORDS.${id}`);
        if (!fs.existsSync(path.join(wordsDir, `${id}.js`))) {
            E(`[#WS5] WORD_ORDER lists '${id}' but words/${id}.js is missing`);
        }
    }

    // (2) Reverse coverage: every file in words/ appears in the manifest.
    for (const f of files) {
        const id = f.replace(/\.js$/, '');
        if (!order.includes(id)) E(`[#WS6] words/${f} not registered in WORD_ORDER`);
    }

    // (3) Language-code integrity. LANG_DATA is already loaded by the
    //     earlier part of the validator into the module-scope global.
    for (const id of order) {
        const w = WORDS[id];
        if (!w || !w.data) continue;
        for (const code of Object.keys(w.data)) {
            if (!LANG_DATA[code]) {
                E(`[#WS7] words/${id}.js references unknown language code '${code}'`);
            }
        }
    }

    // (4) Label / definition completeness.
    for (const id of order) {
        const w = WORDS[id] || {};
        if (!w.label || !w.label.en || !w.label.ja) E(`[#WS8] words/${id}.js missing label.en or label.ja`);
        if (!w.definition || !w.definition.en || !w.definition.ja) E(`[#WS9] words/${id}.js missing definition.en or definition.ja`);
    }

    // (5) Entry shape integrity.
    for (const id of order) {
        const w = WORDS[id];
        if (!w || !w.data) continue;
        for (const [code, val] of Object.entries(w.data)) {
            if (Array.isArray(val)) {
                if (val.length !== 2 || !val[0]) {
                    E(`[#WS10] words/${id}.js: ${code} tuple must be [form, ipa] with non-empty form`);
                }
            } else if (val && typeof val === 'object') {
                if (!val.form) E(`[#WS11] words/${id}.js: ${code} object form must have non-empty .form`);
                if (val.alt && !Array.isArray(val.alt)) E(`[#WS12] words/${id}.js: ${code}.alt must be an array`);
                if (Array.isArray(val.alt)) {
                    for (const a of val.alt) {
                        if (!a.form || !a.script) {
                            E(`[#WS13] words/${id}.js: ${code}.alt entry missing form or script`);
                        }
                    }
                }
            } else {
                E(`[#WS14] words/${id}.js: ${code} must be a tuple or object`);
            }
        }
    }
}
checkPerWordSplit();
// ------------------------------------------------------------------
```

If `vm` and `path` aren't already imported at the top of the file, add them:

```js
const path = require('path');
const vm   = require('vm');
```

- [ ] **Step 2: Run the validator green**

```bash
node validate_wordmap_data.js
```

Expected: zero `[#WSnn]` failures on the migrated state.

If a check fires, inspect the offending file and either fix the data or relax the check (with a comment justifying why).

- [ ] **Step 3: Deliberately break + verify check fires**

Temporarily edit `words/water.js` so `WORDS.water.label.en` is removed.

Run: `node validate_wordmap_data.js`
Expected: `[#WS8] words/water.js missing label.en or label.ja`.

Restore the file (`git checkout words/water.js`) and re-run — should pass.

- [ ] **Step 4: Commit**

```bash
git add validate_wordmap_data.js
git commit -m "Validator: WORDS structural checks (manifest coverage, code integrity, shapes)"
```

---

## Task 11: LANG_CODES.md generator

**Files:**
- Create: `tools/generate_lang_codes_md.mjs`
- Create: `docs/words/LANG_CODES.md` (output)

- [ ] **Step 1: Write the generator**

```js
// tools/generate_lang_codes_md.mjs
//
// Regenerates docs/words/LANG_CODES.md from the current LANG_DATA +
// LANG_NAMES + LANG_DATA[code].meta. Contributors run this whenever
// they add or rename a language entry.
//
//   node tools/generate_lang_codes_md.mjs

import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');

function loadAll() {
    const sandbox = { window: {} };
    vm.createContext(sandbox);
    for (const f of ['wordmap_data.js', 'lang_names.js', 'wordmap_meta.js']) {
        vm.runInContext(fs.readFileSync(path.join(ROOT, f), 'utf8'), sandbox);
    }
    return sandbox;
}

function render(sandbox) {
    const { LANG_DATA, LANG_NAMES } = sandbox;
    const en = (LANG_NAMES && LANG_NAMES.en) || {};
    const ja = (LANG_NAMES && LANG_NAMES.ja) || {};
    const codes = Object.keys(LANG_DATA).sort();
    const rows = codes.map(code => {
        const lang = LANG_DATA[code] || {};
        const meta = lang.meta || {};
        const enName = en[code] || lang.name || '';
        const jaName = ja[code] || '';
        const fam    = meta.family   || '';
        const spk    = meta.speakers || '';
        return `| \`${code}\` | ${enName} | ${jaName} | ${fam} | ${spk} |`;
    });

    return [
        '# LangMap language codes',
        '',
        `Auto-generated by \`tools/generate_lang_codes_md.mjs\` on ${new Date().toISOString().slice(0,10)}.`,
        `Total: ${codes.length} codes.`,
        '',
        '| Code | English name | Japanese name | Family | Speakers |',
        '|------|--------------|---------------|--------|----------|',
        ...rows,
        '',
    ].join('\n');
}

const sandbox = loadAll();
const out = render(sandbox);
const outPath = path.join(ROOT, 'docs', 'words', 'LANG_CODES.md');
fs.mkdirSync(path.dirname(outPath), { recursive: true });
fs.writeFileSync(outPath, out, 'utf8');
console.log(`wrote ${outPath} (${out.split('\n').length} lines)`);
```

- [ ] **Step 2: Run + inspect**

```bash
node tools/generate_lang_codes_md.mjs
head -20 docs/words/LANG_CODES.md
wc -l docs/words/LANG_CODES.md
```

Expected: ~940 lines (header + 934 rows + blank line).

- [ ] **Step 3: Commit**

```bash
git add tools/generate_lang_codes_md.mjs docs/words/LANG_CODES.md
git commit -m "Add LANG_CODES.md generator + initial 934-language reference"
```

---

## Task 12: Validator — LANG_CODES.md staleness check

**Files:**
- Modify: `validate_wordmap_data.js`

- [ ] **Step 1: Add the staleness check**

Append after the `checkPerWordSplit()` call:

```js
function checkLangCodesMdFresh() {
    const file = 'docs/words/LANG_CODES.md';
    if (!fs.existsSync(file)) {
        W(`[#WS15] ${file} not found — run tools/generate_lang_codes_md.mjs`);
        return;
    }
    // Re-run the generator into a temp buffer and compare to disk.
    try {
        const { execSync } = require('child_process');
        execSync('node tools/generate_lang_codes_md.mjs > /tmp/_lang_codes_check.md', { stdio: 'pipe' });
        const fresh   = fs.readFileSync(file, 'utf8');
        const current = fs.readFileSync('/tmp/_lang_codes_check.md', 'utf8');
        if (fresh.trim() !== current.trim()) {
            // Header line carries a date that differs between runs — diff
            // everything except the timestamp line.
            const stripDate = s => s.replace(/Auto-generated.*\n/, '');
            if (stripDate(fresh) !== stripDate(current)) {
                E(`[#WS16] docs/words/LANG_CODES.md is stale — regenerate with: node tools/generate_lang_codes_md.mjs`);
            }
        }
    } catch (e) {
        W(`[#WS17] could not regenerate LANG_CODES.md for staleness check: ${e.message}`);
    }
}
checkLangCodesMdFresh();
```

The generator writes the file in-place; the check compares disk content
against the generator output (with the date line stripped) and flags
drift.

- [ ] **Step 2: Verify it passes**

```bash
node validate_wordmap_data.js
```

Expected: no `[#WS16]` failure.

- [ ] **Step 3: Deliberately stale + verify check fires**

```bash
echo "" >> docs/words/LANG_CODES.md
node validate_wordmap_data.js
```

Expected: `[#WS16] docs/words/LANG_CODES.md is stale …`.

Restore:
```bash
node tools/generate_lang_codes_md.mjs
```

- [ ] **Step 4: Commit**

```bash
git add validate_wordmap_data.js
git commit -m "Validator: LANG_CODES.md staleness check"
```

---

## Task 13: Contributor manual — English

**Files:**
- Create: `docs/words/CONTRIBUTING.md`

- [ ] **Step 1: Write the manual**

```markdown
# Contributing word data to LangMap

This guide is for people adding word translations or new word concepts
to the wordmap. Source data lives in two places:

- `words/<id>.js` — one file per concept (water, fire, etc.). Each file
  contains the concept's label, definition, and per-language translations.
- `wordmap_data.js` — language metadata (lat/lng, native name, etc.).
  Translations no longer live here.

If you only need to fix or add one translation, you can do it without
running anything but a text editor — keep reading.

## Tasks

### Adding a new translation to an existing concept (3 steps)

1. Open the relevant file under `words/`. Concept IDs match filenames
   (`water` ↔ `words/water.js`).
2. Find the region group that fits the language (or add a new comment
   block). Insert the row in the form:

   ```js
   <code>:  ['<surface form>', '<IPA>'],
   ```

   The language code goes on the left; the surface form and IPA on the
   right. Find the right code in [`LANG_CODES.md`](./LANG_CODES.md).

3. Run the validator from the repo root:

   ```bash
   node validate_wordmap_data.js
   ```

   It should report zero failures. If it complains, the message tells you
   what to fix.

### Adding a new concept (5 steps)

1. Pick a unique ID. Use ASCII, lowercase, no spaces (`book`, `river`,
   `to_walk`). Filenames must match the ID.

2. Create `words/<id>.js`. Copy the skeleton at the bottom of this file
   and fill it in. `label.en` and `label.ja` are required; other UI
   languages fall back to English when missing.

3. Append the new ID to `WORD_ORDER` in `word_manifest.js`. Place it in
   the position you want it to appear in the word selector.

4. Bump `WM_ASSET_VERSION.words` in `wordmap.html` so browsers reload
   the new file.

5. Run the validator:

   ```bash
   node validate_wordmap_data.js
   ```

### Adding a new language (rare)

1. Add the language entry to `wordmap_data.js` (name, native, lat/lng).
2. Add localized names to `lang_names.js`.
3. Optionally add metadata (`family`, `speakers`, sources, description)
   in `wordmap_meta.js`.
4. Regenerate the language reference table:

   ```bash
   node tools/generate_lang_codes_md.mjs
   ```

5. Run the validator. It will fail if you forgot the regeneration.

## File format reference

### Simple translation (tuple)

```js
ja: ['水', 'mizu'],
```

Two-element array: `[surface form, IPA]`. IPA can be empty (`''`) if you
don't have it.

### Translation with alternative scripts (object)

When a language uses more than one writing system, switch to the object
form:

```js
za: {
  form:   '淰',                    // primary form shown on the map
  ipa:    'rɐmˀ˧˥',
  alt: [
    { form: 'raemx',    script: 'Latin (Sawcuengh)',
      source: '1957/1982 official orthography' },
    { form: '𭜯 / 淋',  script: 'Sawndip (古壮字)',
      source: 'za.wikipedia.org Raemx' },
  ],
  source: { title: 'za.wikipedia.org', url: 'https://za.wikipedia.org/wiki/Raemx' },
},
```

- `form` and `ipa` describe what shows on the map.
- `alt` is an array of `{form, script, source}` entries — order is
  display order in modals.
- `source` (optional) cites where the primary form came from.

### IPA conventions

- Use IPA notation, not the language's native romanization.
- Tone diacritics belong inline (`ʂweɪ˧˩˧`, `nɨək˧˥`).
- Empty IPA (`''`) is allowed when you only have the surface form.

### Sources

Sources for an entire language belong in `wordmap_meta.js` under that
language's `meta.sources` array.

Sources for an individual word's surface form belong inline in the
object form's `.source` field (or `.alt[i].source` for alternate scripts).

## Skeleton for a new `words/<id>.js`

```js
/**
 * <id> — <one-sentence definition in English>
 *
 * New entries: see docs/words/CONTRIBUTING.md
 * Language codes: see docs/words/LANG_CODES.md
 */
WORDS.<id> = {
  label: {
    en: '<English label>',
    ja: '<Japanese label>',
    // …other UI languages as desired
  },
  definition: {
    en: '<English definition>',
    ja: '<Japanese definition>',
  },
  data: {
    // === East Asia ===
    ja: ['<form>', '<ipa>'],
    ko: ['<form>', '<ipa>'],
    // === Europe ===
    en: ['<form>', '<ipa>'],
    // …
  },
};
```

## Previewing your changes

Run the dev server from the repo root:

```bash
python3 server.py
```

Then open <http://localhost:8001/wordmap.html>. The word selector should
list your new concept (if any) and your new translations should render
on the map.

## Pull-request checklist

- [ ] `node validate_wordmap_data.js` passes
- [ ] If you added a language: `tools/generate_lang_codes_md.mjs` re-run
- [ ] If you added/renamed/reordered concepts: `WM_ASSET_VERSION.words`
      bumped in `wordmap.html`
- [ ] Commit message reads as: `Wordmap: add <X> for <Y>` or similar
```

- [ ] **Step 2: Commit**

```bash
git add docs/words/CONTRIBUTING.md
git commit -m "Add contributor manual for word data (English)"
```

---

## Task 14: Contributor manual — Japanese

**Files:**
- Create: `docs/words/CONTRIBUTING.ja.md`

- [ ] **Step 1: Write the Japanese translation**

```markdown
# LangMap 単語データ寄稿ガイド

このガイドは、ワードマップに単語の翻訳や新しい概念を追加する人向けです。
ソースデータは 2 つの場所に分かれています：

- `words/<id>.js` — 1 ファイル＝1 概念（water、fire など）。各ファイルには
  概念のラベル、定義、言語ごとの翻訳が入っています。
- `wordmap_data.js` — 言語メタ情報（緯度経度、ネイティブ名など）。翻訳
  はここには入りません。

1 つの翻訳を直すだけなら、テキストエディタ以外何も不要です。

## 作業手順

### 既存の概念に新しい翻訳を追加する（3 ステップ）

1. `words/` 配下の該当ファイルを開く。概念 ID とファイル名は一致しています
   （`water` ↔ `words/water.js`）。
2. 言語が属する地域コメントブロックを見つけて（または新規に作成して）、
   以下の形で 1 行追加：

   ```js
   <コード>:  ['<表記>', '<IPA>'],
   ```

   左が言語コード、右が表記と IPA。コードは [`LANG_CODES.md`](./LANG_CODES.md)
   で確認できます。

3. リポジトリのルートで validator を実行：

   ```bash
   node validate_wordmap_data.js
   ```

   失敗が 0 件なら OK。失敗があればメッセージに従って修正してください。

### 新しい概念を追加する（5 ステップ）

1. ユニークな ID を決める。ASCII、小文字、空白なし（`book`、`river`、
   `to_walk` など）。ファイル名は ID と完全一致させます。

2. `words/<id>.js` を作る。このファイル末尾のスケルトンをコピーして埋める。
   `label.en` と `label.ja` は必須、他の UI 言語は英語にフォールバックします。

3. `word_manifest.js` の `WORD_ORDER` に新しい ID を追加。並び順は単語
   セレクタに反映されます。

4. `wordmap.html` の `WM_ASSET_VERSION.words` をインクリメント。ブラウザ
   キャッシュを更新するためです。

5. Validator を実行：

   ```bash
   node validate_wordmap_data.js
   ```

### 新しい言語を追加する（稀）

1. `wordmap_data.js` に言語エントリ（name、native、lat/lng）を追加。
2. `lang_names.js` に翻訳された名前を追加。
3. オプション：`wordmap_meta.js` に系統・話者数・出典・概要を追加。
4. 言語コード対照表を再生成：

   ```bash
   node tools/generate_lang_codes_md.mjs
   ```

5. Validator を実行。再生成を忘れていたらここで失敗します。

## ファイル形式リファレンス

### シンプルな翻訳（タプル）

```js
ja: ['水', 'mizu'],
```

2 要素の配列：`[表記, IPA]`。IPA が不明なら空文字（`''`）で OK。

### 別表記を伴う翻訳（オブジェクト）

複数の文字体系を使う言語はオブジェクト形式で書きます：

```js
za: {
  form:   '淰',                    // 地図上に表示される主表記
  ipa:    'rɐmˀ˧˥',
  alt: [
    { form: 'raemx',    script: 'Latin (Sawcuengh)',
      source: '1957/1982 official orthography' },
    { form: '𭜯 / 淋',  script: 'Sawndip (古壮字)',
      source: 'za.wikipedia.org Raemx' },
  ],
  source: { title: 'za.wikipedia.org', url: 'https://za.wikipedia.org/wiki/Raemx' },
},
```

- `form` と `ipa` は地図表示用。
- `alt` は `{form, script, source}` の配列。モーダルでこの順に表示されます。
- `source`（オプション）は主表記の出典。

### IPA の書き方

- 表記法は IPA。各言語のローマ字表記ではなく国際音声記号で。
- 声調記号は同じ文字列にインライン（`ʂweɪ˧˩˧`、`nɨək˧˥`）。
- 表記だけわかって IPA がないときは空文字（`''`）で構いません。

### 出典の書き方

- **言語全体の出典**は `wordmap_meta.js` の各言語の `meta.sources` 配列。
- **個別の単語表記の出典**はオブジェクト形式の `.source` フィールド（別表記なら
  `.alt[i].source`）。

## 新しい `words/<id>.js` のスケルトン

```js
/**
 * <id> — <英語で 1 文の定義>
 *
 * 新規追加は docs/words/CONTRIBUTING.ja.md
 * 言語コードは docs/words/LANG_CODES.md
 */
WORDS.<id> = {
  label: {
    en: '<英語ラベル>',
    ja: '<日本語ラベル>',
    // …他の UI 言語も任意で
  },
  definition: {
    en: '<英語の定義>',
    ja: '<日本語の定義>',
  },
  data: {
    // === 東アジア ===
    ja: ['<表記>', '<IPA>'],
    ko: ['<表記>', '<IPA>'],
    // === ヨーロッパ ===
    en: ['<表記>', '<IPA>'],
    // …
  },
};
```

## ローカル確認

リポジトリのルートで dev サーバ起動：

```bash
python3 server.py
```

`http://localhost:8001/wordmap.html` を開く。新しい概念があれば単語セレクタに
表示され、新しい翻訳が地図上にレンダリングされるはずです。

## プルリクエストのチェックリスト

- [ ] `node validate_wordmap_data.js` が通る
- [ ] 言語を追加した場合：`tools/generate_lang_codes_md.mjs` を再実行
- [ ] 概念を追加・改名・順序変更した場合：`wordmap.html` の
      `WM_ASSET_VERSION.words` をインクリメント
- [ ] コミットメッセージの形：`Wordmap: <Y>に<X>を追加` 等
```

- [ ] **Step 2: Commit**

```bash
git add docs/words/CONTRIBUTING.ja.md
git commit -m "Add contributor manual for word data (Japanese)"
```

---

## Task 15: Top-level README pointer

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Locate a sensible insertion point**

Open `README.md`. Find a section like "Data structure", "Contributing",
or the file tree.

- [ ] **Step 2: Add a section near the top**

Insert:

```markdown
## Contributing word data

See [docs/words/CONTRIBUTING.md](docs/words/CONTRIBUTING.md) (or the
[Japanese version](docs/words/CONTRIBUTING.ja.md)) for instructions on
adding word translations or new concepts. Language codes are listed in
[docs/words/LANG_CODES.md](docs/words/LANG_CODES.md).
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "README: pointer to per-word contributor docs"
```

---

## Task 16: End-to-end verification

This is a manual smoke pass before declaring the migration done. No code
changes — the tasks above must already be committed.

- [ ] **Step 1: Validator clean**

```bash
node validate_wordmap_data.js
```

Expected: zero `[#WSnn]` and zero new errors. Pre-existing baseline
warnings (`I` infos) can remain.

- [ ] **Step 2: Browser smoke test in 2D**

```bash
python3 server.py    # if not already running
```

Open <http://localhost:8001/wordmap.html>. For each of these concepts,
switch the word selector and confirm the map updates:

  - `water` (every continent should have markers)
  - `one` (numerals; sparser data)
  - `hello` (mixed scripts)
  - `house` (covers languages with `altWordForms` — e.g. Zhuang)

For Zhuang (`za`), open the language info modal and confirm both
`淰` and `raemx`/Sawndip alt forms are visible — proves the object-form
merger landed.

- [ ] **Step 3: Browser smoke test in 3D**

Toggle the 2D/3D button. Repeat the spot checks above.

- [ ] **Step 4: Add a placeholder concept end-to-end**

This validates the "add a concept" flow without keeping the placeholder:

```bash
cat > words/zztest.js <<'EOF'
WORDS.zztest = {
  label: { en: 'Test', ja: 'テスト' },
  definition: { en: 'Plan validation placeholder.', ja: 'プラン検証用。' },
  data: { ja: ['テスト', 'tesɯto'], en: ['test', 'test'] },
};
EOF
```

Append `'zztest',` to `WORD_ORDER` in `word_manifest.js`.
Bump `WM_ASSET_VERSION.words` from 1 to 2 in `wordmap.html`.

Reload the browser. Confirm "Test / テスト" appears in the word selector
and selecting it shows Japan and English markers on the map.

Then revert the placeholder:

```bash
rm words/zztest.js
# undo the word_manifest.js + wordmap.html edits manually (git checkout
# can also reset both files since the changes weren't committed)
git checkout word_manifest.js wordmap.html
```

Re-run the validator — should be back to clean.

- [ ] **Step 5: Optional — push when ready**

```bash
git push origin develop
```

(Don't push without the user's go-ahead.)

---

## Risks / things to watch during execution

1. **`writeStrippedData` regex.** If the inline `words: {…}` or
   `altWordForms: {…}` blocks contain nested braces (they do for the
   trivia panto-action use case but not within these specific fields),
   the regex in Task 4 may eat too much or too little. Watch the
   `node -e "require('./wordmap_data.js')"` smoke test in Task 4 Step 4
   — if it errors, manually inspect the diff and tighten the regex.

2. **WM_ASSET_VERSION exposure timing.** Task 8 Step 1 exposes
   `WM_ASSET_VERSION` to `window` from inside the IIFE. The inline
   loader in Task 7 runs BEFORE that IIFE executes. The loader falls
   back to `1` when `window.WM_ASSET_VERSION` is undefined, which is
   correct for the initial state. After the IIFE runs, subsequent
   reloads pick up the registered version. Don't be alarmed if the
   first-ever load shows `?v=1` regardless of the registry — that's
   only a cosmetic issue while the registry stays at 1.

3. **VM sandbox loading order.** When the validator (Task 10) loads
   every `words/*.js` file into a shared sandbox, file load order is
   filesystem-listing order, NOT manifest order. That's fine because
   each file just writes to its own `WORDS[id]` slot. If you see
   "WORDS.X already defined" warnings, two files share an ID — fix the
   filename.
